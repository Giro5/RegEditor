def changeFocusListbox(event):
    #global table
    #sel = table.index(table.focus())
    #try: table.delete(table.focus())
    #except: return
    #table.insert("", sel, values = (names[sel] if names[sel] != "" else "(Default)", RegTypes[types[sel]], ConvertTypes(types[sel], data[sel])))




def changeFocusTreeview(event):
    #sel = table.index(table.focus())
    #try: table.delete(table.focus())
    #except: pass
    #else: table.insert("", sel, values = (names[sel] if names[sel] != "" else "(Default)", RegTypes[types[sel]], ConvertTypes(types[sel], data[sel])))
    #try: sel = KeysListbox.curselection()[0]
    #except: return
    #selStr = KeysListbox.get(sel)
    #KeysListbox.delete(sel)
    #KeysListbox.insert(sel, selStr)